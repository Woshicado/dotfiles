# Replaces the tmux line this migrated from:
#   launch sh -c 'tmux new-session -A -D -s main -f ~/.config/tmux/tmux.conf'
# `||` is a safety net: if zellij fails, fall back to a login shell
launch sh -c 'zellij attach --create main || exec $SHELL -l'
