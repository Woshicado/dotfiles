local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	height = 40,
	color = colors.bar.bg,
	padding_right = 12,
	padding_left = 12,
	notch_width = 240,
	-- margin = 12,
	-- corner_radius = 20,
})
